{
  "name": "maintenance-tracker-app",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "test": "jest --runInBand"
  },
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "ts-jest": "^29.1.0"
  }
}
